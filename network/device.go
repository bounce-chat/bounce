package network

// When a device connects, see what you need to send and send it
// extract the network ID (public key from the peer address)
// look up the corresponding device and all pending messages for it
// send them

